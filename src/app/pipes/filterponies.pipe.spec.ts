import { FilterponiesPipe } from './filterponies.pipe';

describe('FilterponiesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterponiesPipe();
    expect(pipe).toBeTruthy();
  });
});
